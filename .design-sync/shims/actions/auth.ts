// Preview stub for a 'use server' module — the real one imports next/headers,
// next/cache and the Supabase server client, none of which can run in a
// browser bundle. Each action resolves to a neutral form state so previews
// render the component's idle appearance.

export async function inscrire(): Promise<{ erreur?: string; succes?: string }> {
  return {}
}

export async function connecter(): Promise<{ erreur?: string; succes?: string }> {
  return {}
}

export async function deconnecter(): Promise<{ erreur?: string; succes?: string }> {
  return {}
}

export async function demanderReinitialisation(): Promise<{ erreur?: string; succes?: string }> {
  return {}
}

export async function definirNouveauMotDePasse(): Promise<{ erreur?: string; succes?: string }> {
  return {}
}

