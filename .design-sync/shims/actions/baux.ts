// Preview stub for a 'use server' module — the real one imports next/headers,
// next/cache and the Supabase server client, none of which can run in a
// browser bundle. Each action resolves to a neutral form state so previews
// render the component's idle appearance.

export async function creerBail(): Promise<{ erreur?: string; succes?: string }> {
  return {}
}

export async function modifierBail(): Promise<{ erreur?: string; succes?: string }> {
  return {}
}

export async function resilierBail(): Promise<{ erreur?: string; succes?: string }> {
  return {}
}

export async function reactiverBail(): Promise<{ erreur?: string; succes?: string }> {
  return {}
}

export async function supprimerBail(): Promise<{ erreur?: string; succes?: string }> {
  return {}
}

